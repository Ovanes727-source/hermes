workflows:
  android-debug:
    name: Android Debug Build
    max_build_duration: 30
    environment:
      vars:
        PACKAGE_NAME: "com.example.hermes"
    scripts:
      - name: Show Project Structure
        script: |
          echo "📁 СТРУКТУРА ПРОЕКТА:"
          echo "======================"
          echo "Рабочая директория: $(pwd)"
          echo ""
          echo "Содержимое корня:"
          ls -la
          echo ""
          echo "Все gradle файлы:"
          find . -name "*.gradle*" -type f
          echo ""
          echo "Папки:"
          find . -maxdepth 2 -type d | sort
          
      - name: Check Android Project
        script: |
          echo "🔍 ПРОВЕРКА ANDROID ПРОЕКТА:"
          echo "============================"
          
          # Ищем существующий build.gradle
          echo "1. Ищем существующие build.gradle файлы:"
          find . -name "build.gradle" -o -name "build.gradle.kts" | head -5
          echo ""
          
          echo "2. Ищем settings.gradle:"
          find . -name "settings.gradle" -o -name "settings.gradle.kts"
          echo ""
          
          echo "3. Ищем папку src:"
          find . -type d -name "src" | head -5
          echo ""
          
          echo "4. Ищем AndroidManifest.xml:"
          find . -name "AndroidManifest.xml" | head -5
          echo ""
          
      - name: Build with Existing Gradle
        script: |
          echo "🚀 СБОРКА С СУЩЕСТВУЮЩИМИ ФАЙЛАМИ:"
          echo "=================================="
          
          # Проверяем есть ли нормальный build.gradle
          if [ -f "build.gradle" ] || [ -f "build.gradle.kts" ]; then
            echo "✅ Найден build.gradle в корне"
            echo "Содержимое build.gradle (первые 20 строк):"
            head -20 build.gradle build.gradle.kts 2>/dev/null || true
            echo ""
            
            # Проверяем синтаксис
            echo "🔧 Проверяем синтаксис Gradle..."
            if [ -f "build.gradle" ]; then
              if grep -q "plugins" build.gradle; then
                echo "Найден блок plugins"
                # Исправляем возможные ошибки синтаксиса
                sed -i 's/plugins {/plugins {/g' build.gradle
              fi
            fi
            
            echo "Пробуем собрать..."
            gradle tasks --dry-run 2>&1 | head -20 || true
            echo ""
            
            gradle assembleDebug --stacktrace 2>&1 | tail -50
            BUILD_RESULT=$?
            
            if [ $BUILD_RESULT -eq 0 ]; then
              echo "✅ Сборка успешна!"
            else
              echo "❌ Ошибка сборки"
              # Показываем больше информации об ошибке
              echo "Последние ошибки:"
              gradle assembleDebug --stacktrace 2>&1 | grep -A 10 -B 5 "error\|ERROR\|FAILED\|exception" | tail -30
            fi
            exit $BUILD_RESULT
            
          elif [ -f "app/build.gradle" ] || [ -f "app/build.gradle.kts" ]; then
            echo "✅ Найден app/build.gradle"
            echo "Проверяем структуру..."
            
            if [ -f "settings.gradle" ] || [ -f "settings.gradle.kts" ]; then
              echo "✅ Найден settings.gradle, собираем из корня..."
              gradle assembleDebug --stacktrace
            else
              echo "⚠️ settings.gradle не найден, пытаемся собрать модуль app..."
              cd app
              # Создаем временный settings.gradle
              echo "include ':app'" > ../settings.gradle
              cd ..
              gradle assembleDebug --stacktrace
            fi
            exit $?
            
          else
            echo "❌ Не найден build.gradle файл!"
            echo ""
            echo "📋 Список всех файлов:"
            find . -type f -name "*.java" -o -name "*.kt" -o -name "*.xml" | head -20
            exit 1
          fi
          
      - name: Create Minimal Project if Needed
        script: |
          echo "🛠️ СОЗДАНИЕ МИНИМАЛЬНОГО ПРОЕКТА:"
          echo "================================"
          
          # Проверяем есть ли исходный код
          HAS_SRC=$(find . -type f -name "*.java" -o -name "*.kt" -o -name "AndroidManifest.xml" | head -1)
          
          if [ -z "$HAS_SRC" ]; then
            echo "⚠️ Не найден исходный код Android!"
            echo "Это может быть не Android проект."
            exit 0
          fi
          
          echo "✅ Найден исходный код, создаем минимальную структуру..."
          
          # Создаем правильный build.gradle
          cat > build.gradle << 'EOF'
// Минимальный build.gradle для Android
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

apply plugin: 'com.android.application'

android {
    compileSdk 34
    namespace 'com.example.hermes'
    
    defaultConfig {
        applicationId "com.example.hermes"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }
    
    buildTypes {
        release {
            minifyEnabled false
        }
        debug {
            debuggable true
        }
    }
    
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
EOF
          
          # Создаем settings.gradle если нет
          if [ ! -f "settings.gradle" ] && [ ! -f "settings.gradle.kts" ]; then
            echo "include ':app'" > settings.gradle
          fi
          
          echo "✅ Создан минимальный build.gradle"
          echo "Собираем..."
          gradle assembleDebug --stacktrace --info
          
      - name: Find Results
        script: |
          echo "📦 РЕЗУЛЬТАТЫ:"
          echo "=============="
          
          echo "Ищем APK файлы:"
          find . -type f -name "*.apk" 2>/dev/null | while read file; do
            echo "✅ $file"
            ls -lh "$file"
          done
          
          echo ""
          echo "Ищем папки сборки:"
          find . -type d -name "build" 2>/dev/null | head -5 | while read dir; do
            echo "📂 $dir"
            find "$dir" -name "*.apk" -type f 2>/dev/null | head -3
          done
          
    artifacts:
      - "**/*.apk"
      - "**/build/outputs/apk/**/*"
      - "app/**/*.apk"
      
    publishing:
      email:
        recipients:
          - ovanes727@icloud.com
